spring:
  application:
    name: hello-service-1
  profiles:
    active:
    - dev
  cloud:
    config:
      uri: ${SPRING_CONFIG_URI:http://localhost:8888}
     
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8892/eureka
      
server:
  port: 8090
      